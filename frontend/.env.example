VITE_API_URL=http://localhost:5000/api

# Optional: base URL for the chat Socket.io connection. If unset, it's
# derived from VITE_API_URL by stripping the trailing /api.
VITE_SOCKET_URL=http://localhost:5000
